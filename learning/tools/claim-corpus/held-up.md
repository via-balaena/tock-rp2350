# Sentences that held up

Verbatim or near-verbatim from surfaces that survived review, plus the
corrected forms of the refuted ones. None of these may be flagged.

Measured on git grep 2.54: `^\s*///` matched 16 lines where `^[[:space:]]*///` matched 82.

Why the reader calls the non-strict form rather than `ELF_SECTION_IN_SEGMENT_STRICT` is not established.

`kernel/src/hil/adc.rs` states on seven separate methods that the sample is left-justified in the u16.

A bare `arm-none-eabi-objcopy in.elf out.elf` with no options at all moves `p_filesz` from 0x0 to 0x1500.

The mechanism behind the recurrence has not been isolated; there is one data point.

Both approvals survived the rebase, which was verified by reading the review state through GraphQL after the push.

`chips/sam4l/src/crccu.rs:215` truncates the hardware register with `result as u16`.

All 34 linked board ELFs pass, 33 of them ELF32 and one ELF64.

The branch carries two commits rather than one.

I do not know why the beeper tone comes back high; the start path and every enable path were eliminated.
