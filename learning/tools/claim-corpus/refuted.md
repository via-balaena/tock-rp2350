# Sentences that actually cost something

These are verbatim or near-verbatim from surfaces that were refuted, on
#5156 and elsewhere. Every one must be flagged.

ELF section headers carry no LMA, so BFD reconstructs one on read from file offsets.

The `--set-section-flags .apps=LOAD,ALLOC` gives `.apps` file content, which forces BFD to recompute the program headers.

The rebase will dismiss both approvals, because a review is made against a head and moving the head discards it.

Rebasing the branch dismissed the approvals since GitHub treats a force-push as a new head.

This is the only section whose LMA disagrees with its VMA.

The trigger is the section flags, so changing them is what materializes the content.

Tock never allows dynamic allocation anywhere in the tree.

Because the shell does not word-split, every loop of this shape runs once.
